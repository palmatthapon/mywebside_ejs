<!DOCTYPE html>
<html lang="en">
<head>
 <% include partials/head %>
 <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
 <!--
    <style>
        body {
            padding-top: 80px;
        }
    </style>
 -->
 
</head>
<body>
<% include partials/menu %>

<div class="main container">
    <tr>
        <div class="container">

            <!-- show any messages that come back with authentication -->
            <% if (success_messages.length >0) { %>
                <div class="alert alert-success">
                    <%= success_messages %>
                </div>
                <% } %>

            
            <h1 class="text-left">
                    item List
            </h1>
            
        </div>

        <div class="container-fluid bg-3 text-center" id="item-list">
                <% for(var i=0; i < items.length; i++) { %>
                  <div class="col-md-3 col-sm-4">
                        <div class="thumbnail">
                          <a href="/showitem?itemid=<%= items[i].item_id %>">
                            <img class ="" src="/uploads/<%= items[i].pic1 %>" alt="Image" width="120" height="100%"/>
                          </a>
                          <div class="caption">
                            <a href="/showitem?itemid=<%= items[i].item_id %>"><h5><%= items[i].name %></h5></a>
                            <p>
                                <a href="/item/edit?itemid=<%= items[i].item_id %>" class="btn btn-primary" role="button">Edit</a> 
                                <a href="/item/list/delete?itemid=<%= items[i].item_id %>" class="btn btn-danger glyphicon glyphicon-trash" role="button" onclick="return confirm('Are you sure?')"></a>
                            </p>
                        </div>
                          
                        </div>
                  </div>
                  
                <% } %>
            </div>
            
    </tr>
</div>


</body>
<% include partials/script %>
</html>